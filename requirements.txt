# CUDA 12.8 环境先从 PyTorch 官方 cu128 索引安装 torch，再安装本文件：
#   pip install torch==2.11.0 --index-url https://download.pytorch.org/whl/cu128
#   pip install --no-build-isolation -r requirements.txt

# --- 核心模型依赖 ---
torch==2.11.0
transformers>=5.5.3,<6
accelerate>=0.33
vllm==0.25.1
flash-attn>=2.6

# --- 数据与评测 ---
datasets>=2.20
numpy>=2.2
pandas>=2.2
scikit-learn>=1.4
math-verify>=0.5
sympy>=1.12
pyyaml>=6.0
tqdm>=4.66
matplotlib>=3.8
